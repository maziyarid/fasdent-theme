# Languages

No compiled translation catalog is shipped in Plugin 1.3.3. The plugin loads the `alipasandi-service-content` text domain from this directory so reviewed catalogs can be added later without changing the runtime contract.
